import React from 'react';
import {
    CANTIDAD_PRODUCTO,
    SELECCIONAR_CLIENTE,
    SELECCIONAR_PRODUCTO
} from '../../types';

export default ( state, action ) => {
    
    switch(action.type) {
        
        default:
            return state

    }

}